version: '1.0'

steps:
  initial-clone:
    image: node:latest
    commands:
        - echo "helowowowowofoowfowofwofwwo!!"
  unit-test-step:
    image: node:latest
    working-directory: ${{initial-clone}}
    commands:
        - npm install
        - npm test
  build-step:
    type: build
    image-name: codefresh/express
# push to registry:
#   type: push 
#   candidate: ${{elastic-beanstalk}}
#   tag: latest
